# Person 4 Handoff Contract

Tell the team:

```txt
Report UI is ready at http://localhost:3001.
It reads findings.json from the repo root.
Run Scan, Generate Patch, and Apply Fix buttons call local API routes.
Memory graph is visible in the report.
```

Person 5 needs the report URL and button flow stable for the final demo.
