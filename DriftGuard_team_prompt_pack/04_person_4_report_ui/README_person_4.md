# Person 4 — Report UI + Memory Graph

## Mission

Build the judging screen.

The report UI is where the product becomes understandable. It should look like a real tool, not a debug dashboard.

## Owns

```txt
apps/web
FindingCard
PatchPreview
MemoryGraph
Run Scan button
Generate Patch button
Apply Fix button
Before/After section
```

## Start here

1. Use mock findings first from `../00_shared/05_mock_findings_json.md`.
2. Paste `prompt_01_report_ui.md` into Cursor.
3. Paste `prompt_02_memory_graph.md` for the graph.
4. Paste `prompt_03_api_routes.md` for local command wiring.
5. Use `technical_guide.md` and `acceptance_checklist.md` before handoff.
