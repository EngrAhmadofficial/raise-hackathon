# Person 2 Handoff Contract

Tell the team:

```txt
The scanner is ready.
Run npm run driftguard:scan to write findings.json.
Rules live under apps/demo-saas/.driftguard/rules.
Incidents live under apps/demo-saas/.driftguard/incidents.
The schema is packages/scanner/src/types.ts.
```

Person 3 needs stable finding IDs and file paths.
Person 4 needs valid JSON and patch fields later.
Person 5 needs the command to work from root.
