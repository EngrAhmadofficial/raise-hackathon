# Prompt — Add Before/After Demo Hooks

```txt
Add demo-friendly before/after hooks to apps/demo-saas.

Goal:
The judges should be able to see that the fixed UI remains polished after DriftGuard applies a code patch.

Add:
- a small "Design system status" panel on /billing
- a note that the page uses Button/Card primitives
- comments near the correct Button/Card usage showing the expected pattern

Do not change the drift markers.
Do not remove the intentional drift block.
Do not add backend or auth.
```
