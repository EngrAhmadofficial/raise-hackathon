# Person 1 Handoff Contract

Tell the team:

```txt
The demo SaaS app is ready at apps/demo-saas.
The primary test page is /billing.
The drift block is in apps/demo-saas/src/app/billing/page.tsx.
The exact markers are present:
DRIFTGUARD_DEMO_DRIFT_START
DRIFTGUARD_DEMO_DRIFT_END
Button and Card exist under src/components/ui.
tokens.json exists at apps/demo-saas/tokens.json.
```

Person 2 needs the drift block stable.
Person 3 needs the markers stable.
Person 4 needs the page visually stable for the before/after.
Person 5 needs the page ready for rehearsal.
