# Person 3 Handoff Contract

Tell the team:

```txt
Patch engine is ready.
Run npm run driftguard:patch to generate patch preview.
Run npm run driftguard:fix to apply the deterministic fix.
The patch is written into finding.patch in findings.json.
The source fix targets apps/demo-saas/src/app/billing/page.tsx.
```

Person 4 needs the patch field populated.
Person 5 needs commands stable for demo.
