# Optional Person 6 — Markdown Graph Parser / DriftGraph Owner

Use this only if you actually have six people.

## Mission

Own the local DriftGraph memory layer.

This person should not block the scanner or report UI. If parsing is not done, the report can use a static graph.

## Owns

```txt
markdownRules.ts
graph JSON shape
wiki-link extraction
rule/incidents graph data
MemoryGraph handoff data
```
